import React from 'react';
import "./Header.css"

function Header(): JSX.Element {
    return (
        <div className="Header">
            Header works fine!
        </div>
    );
}

export default Header;